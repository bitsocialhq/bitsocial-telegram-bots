enableScripts: true

# pkc-js releases are consumed same-day; exempt them from yarn's minimum-release-age gate.
# All other packages remain subject to the default gate.
npmPreapprovedPackages:
  - "@pkcprotocol/*"
